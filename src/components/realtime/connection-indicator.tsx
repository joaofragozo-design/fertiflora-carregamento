// Placeholder — será implementado na fase de Realtime
import type { ConnectionStatus } from '@/types'

interface ConnectionIndicatorProps {
  status: ConnectionStatus
}

export function ConnectionIndicator(_props: ConnectionIndicatorProps) {
  return null
}
